const frontCardStyles = (theme) => ({
  card: {
    maxWidth: '600px',
  },
  media: {
    width: 'auto',
    maxWidth: '100%',
    display: 'block',
    margin: 'auto',
    height: 'auto',
    maxHeight: '600px',
    background: theme.palette.common.black,
    borderRadius: '12px',
    objectFit: 'contain',
  },
});

export default frontCardStyles;